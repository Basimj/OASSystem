<div class="ui-avatar" style="@AvatarStyle" aria-label="@AltText">
    @if (!string.IsNullOrWhiteSpace(ImageUrl) && !_imageFailed)
    {
        <img src="@ImageUrl" alt="@AltText" @onerror="ImageFailed" />
    }
    else
    {
        <span aria-hidden="true">@Initials</span>
    }
</div>
