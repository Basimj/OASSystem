@page "/accounting/receipt-vouchers"
@attribute [Authorize]
@inject IAccountingClientService AccountingService
@inject IUiSnackbarService Snackbar
@using OAS.Contracts.Accounting.ReceiptVouchers
@using OAS.Contracts.Accounting.Enums

<PageTitle>سندات القبض | OAS</PageTitle>

<UiAccountingWorkspace>
    <Actions>
        <UiButton Text="سند قبض جديد"
                  IconCssClass="fa-solid fa-plus"
                  Size="ControlSize.Small"
                  OnClick="OpenCreateModal" />
        <UiButton Text="تحديث"
                  IconCssClass="fa-solid fa-rotate"
                  Variant="ButtonVariant.Secondary"
                  Size="ControlSize.Small"
                  Loading="@_loading"
                  OnClick="LoadDataAsync" />
        <UiSearchInput @bind-Value="_searchTerm"
                       Placeholder="ابحث برقم السند أو اسم الدافع..."
                       DebounceMilliseconds="300"
                       OnSearch="SearchChangedAsync" />
    </Actions>

    <Metrics>
        <UiAccountingMetricTile Label="إجمالي السندات" Value="@_totalCount.ToString()" IconCssClass="fa-solid fa-file-invoice" />
        <UiAccountingMetricTile Label="إجمالي المبالغ المقبوضة" Value="@_items.Sum(x => x.TotalAmount).ToString("N2")" IconCssClass="fa-solid fa-coins" Variant="UiAccountingMetricTile.MetricVariant.Debit" />
    </Metrics>

    <Body>
        @if (_loading)
        {
            <UiAlert Message="جاري تحميل سندات القبض..." Tone="AlertTone.Info" />
        }
        else if (_items.Count == 0)
        {
            <UiAlert Message="لا توجد سندات قبض مسجلة." Tone="AlertTone.Warning" />
        }
        else
        {
            @foreach (var v in _items)
            {
                <UiVoucherCard IsReceipt="true"
                               VoucherNumber="@v.VoucherNumber"
                               DateText="@v.VoucherDate.ToString("yyyy-MM-dd")"
                               Amount="@v.TotalAmount"
                               PartyName="@(string.IsNullOrWhiteSpace(v.ReceivedFrom) ? "غير محدد" : v.ReceivedFrom)"
                               PaymentMethodText="@v.PaymentMethod.ToString()"
                               AccountName="الصندوق / البنك"
                               ReferenceNumber=""
                               AllocatedAmount="@v.TotalAmount"
                               StatusText="@v.Status.ToString()"
                               StatusState="@(v.Status == ReceiptVoucherStatus.Posted ? "posted" : "draft")"
                               CanPost="@(v.Status == ReceiptVoucherStatus.Draft)"
                               OnPost="@(() => PostVoucherAsync(v))" />
            }
        }
    </Body>
</UiAccountingWorkspace>

@if (_showCreateModal)
{
    <UiAccountingDialog Title="إنشاء سند قبض جديد" OnClose="CloseCreateModal">
        <ChildContent>
            <UiInputText Label="اسم الدافع" @bind-Value="_createPayerName" />
            <UiAccountingMoneyInput @bind-Value="_createAmount" Currency="ر.س" IsDebit="true" />
            <UiInputText Label="البيان" @bind-Value="_createDescription" />
        </ChildContent>
        <Footer>
            <UiButton Text="حفظ السند" IconCssClass="fa-solid fa-floppy-disk" Loading="@_saving" OnClick="SaveVoucherAsync" />
            <UiButton Text="إلغاء" Variant="ButtonVariant.Ghost" OnClick="CloseCreateModal" />
        </Footer>
    </UiAccountingDialog>
}

@code {
    private bool _loading = true;
    private bool _saving;
    private string _searchTerm = string.Empty;
    private int _pageNumber = 1;
    private int _pageSize = 50;
    private long _totalCount;
    private IReadOnlyList<ReceiptVoucherDto> _items = [];

    private bool _showCreateModal;
    private string _createPayerName = string.Empty;
    private decimal _createAmount;
    private string _createDescription = string.Empty;

    protected override async Task OnInitializedAsync()
    {
        await LoadDataAsync();
    }

    private async Task LoadDataAsync()
    {
        _loading = true;
        try
        {
            var req = new PageRequest { PageNumber = _pageNumber, PageSize = _pageSize, Search = _searchTerm };
            var res = await AccountingService.GetReceiptVouchersPageAsync(req);
            _items = res.Items ?? [];
            _totalCount = res.TotalCount;
        }
        catch (Exception ex)
        {
            Snackbar.Error("تعذر تحميل سندات القبض: " + ex.Message);
        }
        finally
        {
            _loading = false;
        }
    }

    private async Task SearchChangedAsync(string text)
    {
        _searchTerm = text;
        _pageNumber = 1;
        await LoadDataAsync();
    }

    private async Task PostVoucherAsync(ReceiptVoucherDto v)
    {
        var req = new SetReceiptVoucherStatusRequest(ReceiptVoucherStatus.Posted, v.RowVersion);
        var ok = await AccountingService.SetReceiptVoucherStatusAsync(v.Id, req);
        if (ok)
        {
            Snackbar.Success("تم ترحيل سند القبض بنجاح.");
            await LoadDataAsync();
        }
        else
        {
            Snackbar.Error("تعذر ترحيل السند.");
        }
    }

    private void OpenCreateModal()
    {
        _createPayerName = string.Empty;
        _createAmount = 0;
        _createDescription = string.Empty;
        _showCreateModal = true;
    }

    private void CloseCreateModal() => _showCreateModal = false;

    private async Task SaveVoucherAsync()
    {
        if (_createAmount <= 0)
        {
            Snackbar.Warning("يرجى إدخال مبلغ صحيح للسند.");
            return;
        }

        _saving = true;
        try
        {
            var req = new CreateReceiptVoucherRequest(
                DateOnly.FromDateTime(DateTime.Today),
                ReceiptPartyType.Other,
                null,
                _createPayerName,
                PaymentMethod.Cash,
                null,
                null,
                _createAmount,
                _createDescription,
                []);

            var res = await AccountingService.CreateReceiptVoucherAsync(req);
            if (res is not null)
            {
                Snackbar.Success("تم حفظ سند القبض بنجاح.");
                CloseCreateModal();
                await LoadDataAsync();
            }
        }
        catch (Exception ex)
        {
            Snackbar.Error("فشل حفظ السند: " + ex.Message);
        }
        finally
        {
            _saving = false;
        }
    }
}
